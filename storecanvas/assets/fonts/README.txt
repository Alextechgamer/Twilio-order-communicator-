sc-sans.ttf is DejaVu Sans (Bitstream Vera / Arev fonts lineage), free for commercial use.
Used by SC_Print_Ready to rasterize text layers when imagettftext is available.
If missing, composites fall back to GD imagestring (limited size/charset).
